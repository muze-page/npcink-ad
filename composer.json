{
    "name": "npcink/npcink-ad",
    "description": "Create, preview, and publish site-owned WordPress promotions.",
    "type": "wordpress-plugin",
    "license": "GPL-2.0-or-later",
    "require": {
        "php": ">=8.1"
    },
    "require-dev": {
        "dealerdirect/phpcodesniffer-composer-installer": "^1.1",
        "phpstan/phpstan": "^2.1",
        "phpunit/phpunit": "^10.5",
        "szepeviktor/phpstan-wordpress": "^2.0",
        "wp-coding-standards/wpcs": "^3.2"
    },
    "config": {
        "allow-plugins": {
            "dealerdirect/phpcodesniffer-composer-installer": true
        },
        "platform": {
            "php": "8.1.0"
        },
        "sort-packages": true
    },
    "scripts": {
        "test": "phpunit -c phpunit.xml.dist",
        "lint:phpcs": "phpcs -p",
        "lint:phpstan": "phpstan analyse -c phpstan.neon --no-progress --memory-limit=1G",
        "i18n:refresh": "bash scripts/i18n.sh refresh",
        "i18n:check": "bash scripts/i18n.sh check",
        "check": [
            "@test",
            "@lint:phpcs",
            "@lint:phpstan"
        ]
    }
}
