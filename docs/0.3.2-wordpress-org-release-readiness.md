# Npcink Ad 0.3.2 WordPress.org 首发候选记录

- 状态：GO（制品与提交资料已验收；尚未上传 WordPress.org）
- 日期：2026-07-18
- 发布渠道：GitHub Release 与 WordPress.org 首次提交

## 目标与非目标

0.3.2 的目标是把已经通过真实使用的单 Promotion 工作流整理成可审核、可翻译、
可从同一产物发布到 GitHub 和 WordPress.org 的首发候选版本。

本版本不增加广告类型、位置、统计、追踪、Popup、悬浮/吸顶、频控、地理定向、
第二套数据模型或新的一级管理页面，也不改变现有 Promotion 数据和投放判定。

## 变更边界

- 插件头、`NPCINK_AD_VERSION`、`package.json`、PHPStan bootstrap、根目录
  `readme.txt` Stable tag、Git 标签和 WordPress.org SVN 标签统一为 `0.3.2`。
- 根目录 `readme.txt` 收口到 WordPress.org 建议的简明目录说明；历史版本移入
  `changelog.txt`。
- 新增项目自有的英文/简体中文图标、横幅和三组真实界面截图，源 SVG 保留在
  `docs/wordpress-org-submission/source/`；目录 PNG 不进入插件 ZIP。
- 删除 WordPress.org 托管插件不需要的手动 `load_plugin_textdomain()` 调用。
  PHP 简体中文通过 `wp-content/languages/plugins/` 标准语言包路径加载，编辑器
  继续验证完整 JSON 目录；仓库继续维护完整 POT、PO、MO 和 JSON 源文件。
- 发布包新增根目录 `changelog.txt`，继续排除 `.github`、测试、文档、开发依赖、
  源映射和源码编辑器入口。

## 发布门禁

以下门禁全部通过后才能创建 `v0.3.2` 标签：

1. `composer check`：PHPUnit、PHPCS、PHPStan；
2. `pnpm check`：TypeScript、Jest、ESLint、Stylelint；
3. `bash scripts/release-gate.sh`：版本契约、生产构建、翻译完整性、体积预算、
   ZIP 内容、SHA-256 和官方 Plugin Check；
4. WordPress Playground：最低 WP 6.5/PHP 8.1 与当前 WordPress/PHP 组合；
5. 经典主题与区块主题矩阵；
6. 打包插件 Gutenberg E2E；
7. `git diff --check` 与最终 staged diff 审查。

Plugin Check 中 `load_plugin_textdomain` 警告必须消失。允许保留但必须说明的两条
warning 是受控 Promotion `meta_query`，以及授权真实页面预览使用的第三方缓存
约定常量 `DONOTCACHEPAGE`；任何 error 都阻断发布。

## 单一产物规则

`v0.3.2` 标签工作流生成的 `npcink-ad-0.3.2.zip` 与对应 `.sha256` 是唯一发布
产物。GitHub Release、WordPress.org 首次审核上传和后续 SVN `tags/0.3.2/`
必须来自同一标签提交；禁止在本地重新压缩后仍使用 `0.3.2` 名称。

GitHub Actions 完成后必须重新下载 ZIP 与校验文件，独立计算 SHA-256，并确认
发布页产物一致，再提交 WordPress.org。

## WordPress.org 资料

提交表单文案、审核说明、简体中文目录翻译底稿、图片资产和 SVN 清单统一位于
[`docs/wordpress-org-submission/`](wordpress-org-submission/README.md)。图片属于
未来 SVN 顶层 `assets/`，不属于插件 ZIP。

WordPress.org 审核通过后才向 SVN 推送功能代码。`trunk/readme.txt` Stable tag、
`tags/0.3.2/` 中的插件头版本和 GitHub Release 必须保持一致。

## 2026-07-18 首次提交最终验收

结论：`v0.3.2` GitHub Release 中的唯一 ZIP 已达到可以上传 WordPress.org
首次审核的制品标准。本次验收没有重建、重压缩、覆盖或重新发布该 ZIP，也没有
执行 WordPress.org 上传或创建 SVN 内容。

### 唯一制品身份

- Release：<https://github.com/muze-page/npcink-ad/releases/tag/v0.3.2>
- 标签提交：`82df4e0ab5de1a6be60ae7aef25fc8c6b72f9361`
- 文件：`npcink-ad-0.3.2.zip`
- 本次下载路径：`/private/var/folders/1p/rhqz17yx3v5b_jcrlqk8xrnr0000gn/T/npcink-ad-v0.3.2-acceptance.UihM0o/npcink-ad-0.3.2.zip`
- 大小：`124605` bytes（小于 10 MB）
- SHA-256：`0ccaacf5452c316184e852180ce7bf9fb7785f36b9db26f59eed0725512e94de`
- 校验：独立计算值、Release 资产摘要和下载的
  `npcink-ad-0.3.2.zip.sha256` 三方一致。

### 验证证据

| 范围 | 日期与命令/证据 | 结果 |
| --- | --- | --- |
| 改名准备提交前源码门禁 | 2026-07-18：`git diff --check`、`composer check`、`pnpm check` | diff 无错误；324 tests / 757 assertions，PHPCS、PHPStan 通过；8 suites / 138 tests，TypeScript、ESLint、Stylelint 通过 |
| 标签发布门禁 | 2026-07-18：GitHub Actions [Release Gate run 29627459512](https://github.com/muze-page/npcink-ad/actions/runs/29627459512) | 标签提交的 release-gate、当前 Playground、经典/区块主题矩阵和制品上传全部成功；未在当前分支重跑会重建 ZIP 的门禁 |
| Release 下载与校验 | 2026-07-18：`gh release download v0.3.2 --repo muze-page/npcink-ad --pattern 'npcink-ad-0.3.2.zip*'`、`shasum -a 256 -c npcink-ad-0.3.2.zip.sha256` | 下载的 ZIP 校验通过，SHA-256 如上 |
| ZIP 结构 | 2026-07-18：`unzip -t "$PLUGIN_ZIP"` 与路径白名单/禁入项检查 | 60 个条目；唯一顶层目录为 `npcink-ad/`；包含 9 个 `build/` 条目、6 个 `languages/` 条目、`readme.txt`、`changelog.txt`；不含 `.git`、`.github`、`tests`、`docs`、`node_modules`、`vendor`、日志或 `.map` |
| 官方 Plugin Check | 2026-07-18：`PLUGIN_ZIP="$PLUGIN_ZIP" bash tests/plugin-check/run.sh` | Plugin Check 2.0.0 / WordPress 6.5.8：0 errors，仅下列两类既有 warning |
| 最低版本干净安装 | 2026-07-18：`WP_VERSION=6.5 PHP_VERSION=8.1 PLUGIN_ZIP="$PLUGIN_ZIP" tests/playground/run.sh` | WordPress 6.5.8 / PHP 8.1.34：`NPCINK_AD_SMOKE_OK` |
| 当前版本干净安装 | 2026-07-18：`WP_VERSION=latest PHP_VERSION=8.5 PLUGIN_ZIP="$PLUGIN_ZIP" tests/playground/run.sh` | WordPress 7.0.2 / PHP 8.5.5：`NPCINK_AD_SMOKE_OK` |
| 空站浏览器烟雾 | 2026-07-18：`WP_VERSION=7.0 PHP_VERSION=8.5 PLUGIN_ZIP="$PLUGIN_ZIP" tests/e2e/run.sh tests/e2e/first-promotion.spec.ts` | 1/1 passed；进入管理页、创建并发布 Promotion、真实前台展示、暂停后停止展示、恢复和清理均通过 |
| 后台 ZIP 上传 | 2026-07-18：一次性 WordPress 7.0 / PHP 8.5 Playground + Playwright 上传演练 | 从未安装插件的空站上传同一 ZIP；插件列表为 active，Promotion 管理页可进入，Npcink Ad 菜单可见 |
| 简体中文 | 2026-07-18：上述 Playground 包烟雾 | WordPress.org PHP 语言包路径以及两个编辑器 JSON 目录均成功解析；`Promotions` → `推广`、`Npcink Ad delivery` → `Npcink Ad 投放` |
| 元数据与链接 | 2026-07-18：核对 ZIP 内 `npcink-ad.php`、`readme.txt` 与 `submission-copy.md` | `Npcink Ad`、`npcink-ad`、`muze233`、`0.3.2`、`npcink-ad` text domain、WordPress 6.5、PHP 8.1，以及 GitHub 源码/支持链接一致 |

### 已分类且未隐藏的 Plugin Check warnings

1. `WordPress.DB.SlowDBQuery.slow_db_query_meta_query`：仅查询已发布 Promotion，
   当前单 Promotion 模型不值得引入自定义表。
2. `WordPress.NamingConventions.PrefixAllGlobals.NonPrefixedConstantFound`：
   `DONOTCACHEPAGE` 是第三方缓存生态约定，只在已授权真实页面预览且常量尚未定义时设置。

没有第三类 warning，也没有 error。

### 上传前仍由提交人完成

- 确认 `muze233` WordPress.org 账户邮箱当前可用且有人监控；
- 将 `plugins@wordpress.org` 加入白名单并检查垃圾邮件；
- 获得明确上传授权后，上传上述准确 ZIP；
- 审核通过前不创建或推送 WordPress.org SVN 内容。
